# 2026-04-15

- 已基于当前仓库真实结构整理压测方案：首轮先压 API，不压前端渲染；重点覆盖读链路、语义搜索、单条 CRUD、批量导入、Playground 对话与 SSE；核心瓶颈优先关注 Ollama、SQLite 并发写、Qdrant 本地盘 I/O、Webhook/日志附加开销。
- 已核对 mythplus/mem0 的 openmemory 状态机实现：记忆主状态由后端 `Memory.state` 持久化为 `active/paused/archived/deleted`，并记录 `archived_at`、`deleted_at` 与 `memory_status_history`；前端/MCP 只负责触发，删除还会先删向量库，再落库为 deleted。
- 已对比 demo2 与 openmemory 的状态设计：demo2 已实现四态与状态动作接口，但目前仍是关系库 `state` 与 Qdrant `metadata.state` 双写并存；结构化列表已主要走 `meta_service`，搜索/单条读取/Playground 仍部分依赖 Qdrant metadata。
- 已形成 demo2 状态统一改造建议：以 SQLite `MemoryMeta.state` 作为唯一真相源，先统一搜索/Playground/单条读取的状态判定，再逐步去除对 Qdrant `metadata.state` 的业务依赖，仅保留兼容同步或最终下线。
- 修复记忆管理页面 bug：`build_memory_filter` / `iter_memories_raw` / `get_all_memories_raw` 三处补齐 `exclude_states` 参数传递，修复非分页链路状态排除失效问题；更新 `resolve_list_state_filters` 注释与物理删除设计一致。
- 重构 Playground 对话：用 LangGraph StateGraph 替代线性函数，拆为 `retrieve_memories` → `generate_reply` → `store_memories` 三节点；非流式走完整状态图 `ainvoke`，流式走混合模式（节点检索 + 手动 SSE + 后台存储）；已安装 `langgraph>=1.1.6` + `langchain-core>=1.2.29` 并更新 requirements.txt。







