# 2026-04-03 工作日志

## 项目全面审计
- 完成 demo2 项目安全与质量审计，覆盖后端 server.py(2290行) + 前端 72 文件
- 发现 4 个严重(P0)、9 个中等(P1)、6 个低优(P2) 问题
- P0: 零认证、敏感信息硬编码、无分页限制、文件上传无限制
- P1: 错误泄露内部实现、sync阻塞async、Neo4j无连接池、N+1查询、无请求超时、URL参数未编码、删除静默失败、空catch块、生产配置不安全
- P2: 代码重复、TypeScript any滥用、组件过大
- 功能完成度约90%，安全生产就绪度约30%
- 报告输出至 brain/project-audit-report.md

## 优化方案编写
- 编写了完整的三轮优化方案（brain/optimization-plan.md）
- 第一轮（安全）: API Key认证、CORS白名单、敏感信息脱敏、文件上传限制、错误信息脱敏
- 第二轮（性能）: Qdrant分页滚动、统计O(N)优化、httpx异步HTTP、Neo4j单例驱动、前端请求超时、URL编码、toast通知、生产模式控制
- 第三轮（质量）: metadata回写公共函数、Graph构建提取、组件拆分、TypeScript类型
- 关键发现: config.yaml 已定义 security 配置（api_key, cors_origins, rate_limit）但 server.py 从未读取使用，属于"配了但不生效"的死代码

## 优化实施（第一轮安全 + 第二轮性能）

### 后端 server.py 修改
1. ✅ `_get_all_memories_raw()`: limit=200 → 完整分页滚动（while loop + offset）
2. ✅ `delete_all_memories`: limit=1000 → 循环分页删除，确保全部清空
3. ✅ 统计接口 `get_stats`: O(30*N) 嵌套循环 → O(N) 单次遍历 + 哈希查表
4. ✅ fallback 配置: 移除硬编码 IP/密码，改用 `os.environ.get()` 从环境变量读取
5. ✅ 全局异常处理器: `@app.exception_handler(Exception)` 防止未捕获异常泄露堆栈
6. ✅ `test_llm_connection` / `test_embedder_connection`: 从每次新建 `httpx.AsyncClient` 改为复用全局 `_http_client` 单例
7. ✅ 全局 `_http_client`: 在 lifespan 中初始化/关闭，替代阻塞的 sync requests
8. ✅ 错误消息脱敏: `str(e)` → `_safe_error_detail(e)`

### 前端修改
1. ✅ URL 编码: 7 个方法的路径参数全部加 `encodeURIComponent`（getMemory/updateMemory/deleteMemory/deleteAllMemories/getMemoryHistory/getRelatedMemories/getAccessLogs）
2. ✅ 文件上传限制: 10MB 文件大小 + 1000 条记录上限（import-dialog.tsx）
3. ✅ 删除失败 toast 通知: memories/page.tsx + users/[userId]/page.tsx + memory/[id]/page.tsx 三个页面
4. ✅ 请求超时: 确认已有 30 秒全局超时（之前已实现）

### 新增文件
- `.env.example`: 环境变量模板，包含 Ollama/Neo4j/API Key/端口/环境模式配置
