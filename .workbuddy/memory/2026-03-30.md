# 2026-03-30 工作日志

## 请求监控功能修复
- **后端**：确认中间件 `RECORD_METHODS = {"POST", "PUT", "DELETE"}` 只记录前端写操作，跳过 GET/OPTIONS/HEAD
- **后端**：精简 `_classify_request()`，移除 GET 请求分类（统计/查询/关联/历史/日志/健康检查），只保留4种业务类型：添加、搜索、删除、更新
- **后端**：`SKIP_PATHS` 增加 `"/"` 防止健康检查被记录
- **前端**：精简 `TYPE_COLORS` 和 `TYPE_HEX`，只保留4种业务类型
- **前端**：筛选下拉改为动态合并 `DEFAULT_TYPES` + `stats.types`，去重后展示，每个选项前带颜色圆点
- **数据清理**：删除 request_logs 表中21条非业务类型旧数据（查询9/统计6/OPTIONS4/GET2），剩余4条正确记录

## 排查：添加记忆后无监控记录
- 原因：修改 server.py 后 uvicorn watchfiles reload 期间中间件暂时不可用，请求被处理但日志未记录
- 验证：手动发 POST /v1/memories/ 测试，中间件正常记录到 request_logs
- 结论：reload 完成后一切正常，无需代码修复

## 记忆详情页优化
- **基本信息布局**：用户ID从grid并排改为放到记忆ID下方，纵向排列更清晰
- **修改历史标签显示**：后端 history 接口增强，返回当前 categories；前端在每条历史事件下方展示标签
- **MemoryHistory 类型**：增加可选 `categories?: string[]` 字段
- 同步更新了侧边面板(`memory-detail-panel.tsx`)和独立详情页(`memory/[id]/page.tsx`)

## 记忆详情基本信息重排
- 侧边面板排序改为：状态 → 记忆ID → 用户ID → 分类标签 → 记忆内容 → Hash → 时间 → Agent ID → 元数据
- 所有字段标签字体统一为 `text-xs font-medium text-muted-foreground`

## 修改历史标签快照机制
- **Bug**：之前 history 接口把当前 categories 统一塞给所有历史记录，导致新增时只有1个标签也显示成修改后的3个
- **修复**：新增 `category_snapshots` 表（SQLite），记录每次标签变更的快照
- **add_memory**：写入 Qdrant 后记录初始标签快照
- **update_memory**：metadata 更新后记录新标签快照
- **history 接口**：根据每条历史的 `created_at` 时间匹配 `<= 该时间` 的最新快照，精确还原每个时间点的标签
