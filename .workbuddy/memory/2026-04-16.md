# 2026-04-16

- 重写仪表盘图表组件 `stats-charts.tsx`：从饼图+单折线图改为两个并排面板（新增记忆趋势 + 请求趋势），参考 mem0 官方 dashboard；新建 Switch UI 组件。
- 修复请求面板细分逻辑反转、total 字段缺失问题；底部图例从 recharts Legend 改为自定义标签布局。
- 后端 `classify_request` 增加 Playground 路径匹配 → 分类为「对话」；前端请求日志页加「对话」tab + 紫色 Badge。
- 修复遗漏：将 `access_logs.db` 中 2 条历史 `POST` 类型记录批量 UPDATE 为「对话」，消除仪表盘图例残留。
- 新增日志自动清理机制：所有日志表统一保留 30 天，超期自动删除；启动时清理一次 + 每天定时清理；清理后尝试 VACUUM 回收空间。涉及 `log_service.py`（`cleanup_old_logs` + `start_log_cleanup`）和 `app.py`（lifespan 注册）。
