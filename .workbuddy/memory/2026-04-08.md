# 2026-04-08

## 前端 UI 一致性审计 + 修复

### 修复内容（7个文件）
**P0 修复：**
- `users/page.tsx`: space-y-4 → space-y-6，统计卡片 padding 对齐 Dashboard
- `data-transfer/page.tsx`: space-y-4 → space-y-6
- `memory/[id]/page.tsx`: space-y-3 → space-y-6
- `graph-memory/page.tsx`: h1 → h2 + 补 tracking-tight（两处）
- `requests/page.tsx`: 描述去掉多余 text-sm mt-1

**P1 修复：**
- `requests/page.tsx`: Tab/筛选按钮/快捷按钮从 blue 硬编码色 → primary CSS 变量
- `search/page.tsx`: 清除按钮从红色硬编码 → 标准 Button outline
- `users/page.tsx`: 清除按钮从原生 button 红色 → 标准 Button outline
- 创建公共 `components/shared/user-combobox.tsx`，替换 search 和 data-transfer 的手写下拉
- 删除 search/page.tsx 底部 77 行的旧 UserSearchDropdown 定义

**P2 修复：**
- `requests/page.tsx`: 空状态 py-16 → py-12，骨架屏 rounded → rounded-lg
- `data-transfer/page.tsx`: 原生 table → shadcn Table 组件

## 编辑记忆弹窗溢出修复
- `edit-memory-dialog.tsx`: 所属用户长文本溢出导致弹窗被撑开
  - 加 `overflow-hidden` + `break-all` + `line-clamp-2` 处理长用户ID
  - 弹窗加 `max-h-[85vh] overflow-y-auto` 防内容超屏
- `dialog.tsx`: DialogContent 底层加 `overflow-hidden` 兜底

## 简约设计系统全面改造
**风格**: Swiss Modernism + Data-Dense Dashboard，Inter 字体，Indigo 主色调

### 全局样式改造
- `globals.css`: 全新配色系统（Indigo 主色 #4F46E5，中性灰基底），极细滚动条，抗锯齿
- `tailwind.config.js`: 添加 Inter 字体族优先

### 公共 UI 组件改造（8个）
- `card.tsx`: rounded-lg → rounded-xl，更轻阴影，更紧凑 padding (p-6→p-5)，CardTitle text-2xl → text-sm
- `button.tsx`: rounded-md → rounded-lg，h-10 → h-9，加 cursor-pointer + shadow-sm
- `badge.tsx`: font-semibold → font-medium，text-xs → text-[11px]，px-2.5 → px-2
- `input.tsx`: rounded-md → rounded-lg，h-10 → h-9，ring-2 → ring-1
- `table.tsx`: TableHead 大写+tracking-wider+更小字号，TableCell padding 统一
- `sidebar.tsx`: w-58 → w-56，图标 h-5 → h-[18px]，text-sm → text-[13px]，Logo 精简
- `header.tsx`: h-14 → h-12，backdrop-blur，连接状态极简化
- `client-layout.tsx`: 页面 padding 统一 p-4/md:p-6/lg:p-8

### 页面改造
- `page.tsx (Dashboard)`: 统计卡片标题大写+tracking-wide，骨架屏独立，hover:shadow-sm
- 所有页面描述文字统一加 `text-sm`
