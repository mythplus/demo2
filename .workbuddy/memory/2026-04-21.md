# 2026-04-21

- 修复 Playground "新增 X 条记忆"计数虚高问题：Mem0 SDK 的 `m.add(...)` 返回的 `results` 里每项带 `event` 字段（ADD / UPDATE / DELETE / NONE），之前后端 `store_memories_node` 把 ADD 和 UPDATE 一起塞进 `new_memories`，前端气泡又直接 `.length` 显示"新增"，导致改写已有记忆也被算成"新增"（实测 2 条真新增被显示成 7 条）。修正：①前端 `playground/page.tsx` 按 `event` 分桶，ADD 才叫"新增 X 条记忆"（emerald 绿 + Plus 图标），UPDATE 单独叫"更新 X 条记忆"（sky 蓝 + RefreshCw 图标）；②后端 `routes/playground.py` 提前 `continue` 跳过非 ADD/UPDATE 事件和缺 id 的无效事件，口径收紧。前后端 lint 零报错。
