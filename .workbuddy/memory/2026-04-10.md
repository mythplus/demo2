# 2026-04-10

## Playground 页面 UI 风格统一
- 清理了 playground/page.tsx 中重复定义的 MemorySidebar 和 PlaygroundPage 组件（原文件 1198 行有两份完整实现）
- 统一页面标题区域：使用 `<h2 className="text-2xl font-bold tracking-tight">` + 副标题，与仪表盘、记忆管理等页面一致
- 对话主体区域用 Card 组件包裹，保持项目统一的卡片风格
- 使用 `space-y-6` 间距系统，与其他页面一致
- 操作按钮样式统一为 `variant="outline" size="sm" className="h-8 gap-1.5"` 格式

## Playground 页面 UI 细节调整
- 侧边栏调试台移到仪表盘正下方（第2位）
- 记忆面板按钮移到用户选择栏右侧
- header 加上 playground → "调试台" 映射
- 清空对话按钮移到用户选择栏右侧，并加大尺寸
- "对话用户"文字改为黑色，默认值从 default_user 改为灰色占位"选择用户"
- 去掉"AI 正在回复..."提示
- 侧边栏 Logo 区域高度从 h-14 改为 h-12，与右侧 header 对齐

## 表格用户列溢出修复
- memory-filters.tsx: 用户下拉按钮文本加 truncate + max-w-[120px]
- memory-filters.tsx: 筛选摘要 Badge 加 max-w-[200px] truncate
- memory-table.tsx: 改为百分比列宽 + table-fixed 布局，用户列和分类列不再重叠

## 全局 UI 间距密集化优化
- client-layout.tsx: main 区域 padding 从 `p-4 md:p-6 lg:p-8` 缩小为 `p-3 md:p-4`
- 所有 9 个页面: `space-y-6` → `space-y-4`，页面标题从 `text-2xl` → `text-xl`
- 记忆管理 + 语义检索筛选栏: `CardContent pt-6` → `pt-4`
- 仪表盘底部 grid: `gap-6` → `gap-4`
- Playground Card 高度: `calc(100vh - 13rem)` → `calc(100vh - 11rem)`

## Webhooks 配置页面（前端预览）
- 新建 src/app/webhooks/page.tsx：完整的 Webhook 管理页面
- 功能：添加/编辑/删除 Webhook、启用/禁用开关、事件类型选择（4种）、签名密钥、复制URL、模拟测试
- 侧边栏添加 Webhooks 导航（数据导出下方），header 添加标题映射

## Webhooks 后端实现
- 新建 server/services/webhook_service.py：
  - JSON 文件存储（webhooks.json）
  - CRUD 操作
  - 企业微信群机器人自动适配（检测 qyapi.weixin.qq.com URL，使用 markdown 消息格式）
  - 通用 HTTP POST 推送 + HMAC-SHA256 签名
  - 异步触发（asyncio.ensure_future，不阻塞主请求）
- 新建 server/routes/webhooks.py：
  - GET/POST/PUT/DELETE /v1/webhooks/ CRUD
  - POST /v1/webhooks/{id}/toggle 启用/禁用
  - POST /v1/webhooks/{id}/test 测试推送
- memories.py 中 add/update/delete 操作后嵌入 webhook 触发
- 前端 Webhooks 页面改为调用后端 API（去掉 localStorage）
- API 客户端 client.ts 添加 webhook 相关方法
