# 2026-04-10

## Playground 页面 UI 风格统一
- 清理了 playground/page.tsx 中重复定义的 MemorySidebar 和 PlaygroundPage 组件（原文件 1198 行有两份完整实现）
- 统一页面标题区域：使用 `<h2 className="text-2xl font-bold tracking-tight">` + 副标题，与仪表盘、记忆管理等页面一致
- 对话主体区域用 Card 组件包裹，保持项目统一的卡片风格
- 使用 `space-y-6` 间距系统，与其他页面一致
- 操作按钮样式统一为 `variant="outline" size="sm" className="h-8 gap-1.5"` 格式

## Playground 页面 UI 细节调整
- 侧边栏调试台移到仪表盘正下方（第2位）
- 记忆面板按钮移到用户选择栏右侧
- header 加上 playground → "调试台" 映射
- 清空对话按钮移到用户选择栏右侧，并加大尺寸
- "对话用户"文字改为黑色，默认值从 default_user 改为灰色占位"选择用户"
- 去掉"AI 正在回复..."提示
- 侧边栏 Logo 区域高度从 h-14 改为 h-12，与右侧 header 对齐

## 表格用户列溢出修复
- memory-filters.tsx: 用户下拉按钮文本加 truncate + max-w-[120px]
- memory-filters.tsx: 筛选摘要 Badge 加 max-w-[200px] truncate
- memory-table.tsx: 改为百分比列宽 + table-fixed 布局，用户列和分类列不再重叠

## Playground 自动分类功能
- 修改 server/routes/playground.py，在 m.add() 后对新增记忆调用 auto_categorize_memory
- 新增 _write_categories_to_qdrant 辅助函数，将分类结果直接写入 Qdrant metadata
- 同时给新增记忆设置 metadata.state = "active"
