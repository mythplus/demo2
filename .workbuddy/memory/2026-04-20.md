# 2026-04-20

- 对 demo2 前端与 Dockerfile 做了一轮全面审查：前端架构总体规范（Next.js 14 + TS + shadcn + Zustand + React Query，standalone 输出、健康轮询引用计数、lucide tree-shaking 都做到位），发现的主要问题是 Docker 侧：①缺 `mem0-dashboard/.dockerignore`，`COPY . .` 会把 `node_modules/.next/.env.local` 带进构建上下文；②`docker-compose.yml` 的 `MEM0_WORKERS` 默认 2 与后端"生产单 worker 避免 SQLite 锁竞争"的策略矛盾；③后端 Dockerfile 把 pytest 打进了运行镜像。其他 P1 项：`next.config.js` 无用的 `/api/mem0` rewrites 可删、后端 healthcheck 可改到 `/v1/health`、前后端镜像可加 `VOLUME` 与锁 node 版本。报告已落盘到 artifact。
- 一次性修复 P0+P1：新建 `mem0-dashboard/.dockerignore`；`docker-compose.yml` 的 `MEM0_WORKERS` 默认值改为 1 并删除已废弃的 `version: "3.8"`；拆分 `requirements.txt`（运行时）与新增 `requirements-dev.txt`（含 pytest）；后端 Dockerfile 只装运行时依赖、加 `VOLUME ["/app/qdrant_data","/app/data"]`、healthcheck 改打 `/health` 并校验 200；前端 Dockerfile 锁定 `node:18.20.4-alpine`；删 `next.config.js` 中无用的 `/api/mem0` rewrites；重写根 `.dockerignore`（排除 `mem0-dashboard/` 整目录、补 rate_limit/memory_meta/webhooks.json 等本地运行时文件）；compose 里后端 healthcheck 也对齐到 `/health`。前端 `npm run lint` 通过（exitCode=0，仅剩历史 warning）。
- 落地工蜂（Git@Work）CI/CD 方案：新增 `.ci/stream.yml`（test / build-image / deploy-prod 三阶段，Stream v2.0 格式，镜像仓库走 CSIGHUB，凭据走工蜂凭据管理）与 `docker-compose.prod.yml`（生产部署改拉远程镜像，支持 `BACKEND_IMAGE_TAG/FRONTEND_IMAGE_TAG` 变量切换版本）。test 阶段利用项目现有 `tests/conftest.py` 的 mock（Mem0/Qdrant/Neo4j/log_service 都已 mock），CI 不需真连外部服务。另输出了完整落地指南 artifact，涵盖凭据配置、CSIGHUB 仓库准备、服务器首次部署、日常 PR/tag 流程、可选切蓝盾部署与排错小抄。
- 继续补空结果导出：通过页面实际选择"全部用户"并将日期范围设为 `2099-01-01`~`2099-01-02`，预览已收敛为 `共 0 条数据`，随后空结果 JSON 导出成功，记录区新增 `mem0-export-20260417_1639.json`。
- 新增 preflight 快速体检脚本：`scripts/preflight.ps1`（Windows 主力）+ `scripts/preflight.sh`（Linux/macOS 保底）。覆盖后端 pytest、前端 lint+build+jest、以及 `.env.local` 是否被追踪、`.dockerignore` 关键项是否齐全、`config.yaml` 是否存在 3 项静态体检。**重要踩坑**：PowerShell 默认用 ANSI/GBK 读脚本文件，但 write_to_file 写入的是 UTF-8，会把中文字符串变成乱码令 PS parser 抛 "Unexpected token '}'" 错。解决办法：PS 脚本全部用 ASCII（英文注释 + 英文提示）。Bash 版保持中文无问题。
- 设置页扩展：因为 Qdrant 和 PostgreSQL 都已切换为远程服务，前端设置页的"向量数据库"和（新增的）"元数据库"卡片现在像 LLM/Embedder 那样显示基本信息并带测试按钮。后端新增 `/v1/config/test-vector`（mock Qdrant get_collection 返回 points_count + dimensions + 维度一致性校验）与 `/v1/config/test-meta`（SQLAlchemy engine.connect 执行 `SELECT 1 / version() / COUNT memory_meta`，返回耗时/版本/行数）；`/v1/config/info` 补 `vector_store.url` 与完整 `meta_store { provider host port database url }` 块，URL 统一剥离 userinfo 后生产环境再对 IP 做脱敏。10 个新 pytest 用例全绿。**踩坑**：本地 Python 3.14 + psycopg2-binary 2.9.10 无 wheel 且无法编译 → 在 `tests/test_health.py` 模块顶层通过 `sys.modules` 注入 `psycopg2/psycopg2.extras/psycopg2.pool` stub（用 `types.ModuleType` 真模块），规避 conftest 里 `server.app` import 时的 `ModuleNotFoundError`。
