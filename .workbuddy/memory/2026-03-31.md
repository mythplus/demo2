# 2026-03-31

## 暗色模式修复 - data-transfer 页面
- 修复导入导出页面中多个组件在暗色模式下显示白色背景的问题
- 涉及: 导出/导入 Badge (bg-blue-50/bg-red-50)、"已启用" Badge (bg-green-100)、"查看导入进度"按钮、"查看导入结果"按钮
- 统一添加 dark: 变体，使用 900/30 透明背景 + 400 色文字 + 800 色边框

## 暗色模式修复 - 仪表盘饼图白边框
- 分类分布和状态分布的 PieChart Cell 默认 stroke="#fff"，暗色下可见白边
- 修复: 给两个饼图的 Cell 加 stroke="none"

## 侧边栏子路由高亮修复
- 问题: /users/[userId] 页面侧边栏"用户管理"不高亮，因为用了 pathname === item.href 严格匹配
- 修复: 改为 startsWith 前缀匹配，仪表盘"/"保持精确匹配

## 全盘暗色模式修复 (13个文件约36处)
- memory-filters.tsx: 清除按钮5个红色硬编码全部加 dark 变体
- import-dialog.tsx: 黄色警告文字、成功/失败图标和数字加 dark 变体 (6处)
- data-transfer/page.tsx: 操作汇总成功/导入中/失败状态文字加 dark 变体 (3处)
- page.tsx(仪表盘): 趋势数字 text-green-600 加 dark 变体
- settings/page.tsx: 连接成功/失败文字加 dark 变体 (2处)
- requests/page.tsx: 状态成功/失败图标加 dark 变体 (2处)
- memory/[id]/page.tsx: 复制成功图标加 dark 变体
- 注: bg-*-500 状态圆点(header/错误提示等)在暗色下仍可见，保持不变
