# 2026-04-22

- 输出 DEPLOY.md 部署指南，覆盖三种部署方式（本地裸跑 / Docker 一键 / 生产远程镜像）、外部依赖说明（Ollama/PG/Qdrant/Neo4j）、配置变量速查表、alembic 迁移命令、常见问题 8 条。

- 修复深色模式下 `<input type="date">` 日历图标/文字不可见问题（记忆管理筛选面板 + 数据导出页面）：根因是浏览器原生控件颜色随系统 color-scheme 控制，CSS 没声明则默认 light，深色背景下图标与背景近色融合。修复三处：① `globals.css` 在 `:root` 声明 `color-scheme: light`，`.dark` 声明 `color-scheme: dark`，并加 `::-webkit-calendar-picker-indicator` 深色下 `filter: invert(1) brightness(1.4)` 图标反白；② `memory-filters.tsx` 两个 date Input 加 `dark:bg-[hsl(224_14%_14%)] dark:[color-scheme:dark]`；③ `data-transfer/page.tsx` 两个 date Input 同样处理。lint 0 报错。
